{
    "name": "tenkawa-php",
    "displayName": "Tenkawa PHP",
    "description": "Autocompletion for PHP",
    "version": "0.0.9",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/tsufeki/vscode-tenkawa-php.git"
    },
    "publisher": "tsufeki",
    "keywords": [
        "php",
        "autocompletion",
        "multi-root ready"
    ],
    "engines": {
        "vscode": "^1.19.0"
    },
    "categories": [
        "Languages"
    ],
    "activationEvents": [
        "onLanguage:php",
        "workspaceContains:**/*.php"
    ],
    "main": "./out/extension",
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "install:composer": "rm -rf tenkawa-php-language-server 2>/dev/null; composer create-project --no-dev --remove-vcs --no-interaction tsufeki/tenkawa-php-language-server tenkawa-php-language-server dev-master",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "dependencies": {
        "vscode-languageclient": "^4.0.0"
    },
    "devDependencies": {
        "typescript": "^2.6.1",
        "vscode": "^1.1.6",
        "@types/node": "^7.0.43"
    },
    "contributes": {
        "configuration": {
            "type": "object",
            "title": "Tenkawa PHP configuration",
            "properties": {
                "tenkawaphp.executablePath": {
                    "type": [
                        "string",
                        "null"
                    ],
                    "default": null,
                    "description": "The path to a PHP >=7.0 executable."
                }
            }
        }
    }
}
